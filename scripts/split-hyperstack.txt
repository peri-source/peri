dir = getDirectory("Output directory");
name = getTitle();
Stack.getDimensions(width, height, channels, slices, frames);
setBatchMode(true);
for (f = 1; f <= frames; f++){
    Stack.setFrame(f);
    run("Reduce Dimensionality...", "slices keep");
    saveAs("Tiff", dir+name+"_t"+IJ.pad(f,3)+".tif");
    close();
} 
